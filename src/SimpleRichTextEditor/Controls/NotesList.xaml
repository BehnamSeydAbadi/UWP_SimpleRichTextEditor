<UserControl
    x:Class="SimpleRichTextEditor.Controls.NotesList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleRichTextEditor.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <ListView x:Name="NotesListView"
              Background="Transparent"
              SelectionMode="None"
              IsItemClickEnabled="True"
              ItemClick="NotesListView_ItemClick"
              ItemsSource="{Binding Notes, ElementName=root}">

        <ListView.ItemTemplate>
            <DataTemplate>
                <local:NoteListItem Note="{Binding}" />
            </DataTemplate>
        </ListView.ItemTemplate>

    </ListView>

</UserControl>
