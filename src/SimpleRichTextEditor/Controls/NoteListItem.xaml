<UserControl
    x:Class="SimpleRichTextEditor.Controls.NoteListItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleRichTextEditor.Controls"
    xmlns:models="using:SimpleRichTextEditor.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Border Background="White" 
            BorderBrush="#DDDDDD" 
            BorderThickness="0,0,0,1" 
            Padding="12">

        <StackPanel>
            <TextBlock Text="{Binding Note.Title, ElementName= root}" 
                       FontSize="19" 
                       FontWeight="SemiBold" 
                       Foreground="Black" 
                       TextTrimming="CharacterEllipsis" />

            <TextBlock Text="{Binding Note.Content, ElementName= root}" 
                       FontSize="14" 
                       Foreground="Gray" 
                       Margin="0,6,0,0" 
                       TextWrapping="NoWrap" 
                       TextTrimming="CharacterEllipsis" />

            <TextBlock Text="{Binding Note.ModifiedAt, ElementName= root}"
                       FontSize="12"
                       Foreground="DarkGray"
                       Margin="0,8,0,0"/>
        </StackPanel>

    </Border>

</UserControl>
